services:
  - type: web
    name: app3monkeys
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn project3monkeys.wsgi:application
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: project3monkeys
          property: connectionString
databases:
  - name: project3monkeys
    plan: free

