services:
  - type: web
    name: app3monkeys
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn project3monkeys.wsgi:application
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
